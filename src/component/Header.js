import React from 'react';

import '../sass/main.scss';

const Header = () => {
    return (
      <header>
        <nav className="container">
          <h1>To-Do List</h1>

        </nav>
      </header>
    );
}

export default Header;
